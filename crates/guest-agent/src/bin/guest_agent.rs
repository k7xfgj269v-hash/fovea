// guest-agent 二进制 stub —— M2 起接 MCP server。M1 阶段先空骨架让 workspace 编译可生成 bin。

fn main() {
    // 占位：M1 第一刀不需要跑 daemon。
    eprintln!("guest-agent: M1 第一刀进度，无 daemon 逻辑；see crates/guest-agent/src/lib.rs");
}
